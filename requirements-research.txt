# Research-Topic Implementation Requirements
# Version: 1.0.0-alpha
# Created: 2025-11-30

# Core Dependencies (REQUIRED)
aiohttp>=3.9.0           # Async HTTP client for API calls
pydantic>=2.0.0          # Data validation and schemas
beautifulsoup4>=4.12.0   # HTML parsing for article extraction
lxml>=4.9.0              # XML/HTML parser (used by BeautifulSoup)

# Logging (Already in project)
# structlog  # Structured logging (should already be installed)

# Optional: Content Generation (TO BE IMPLEMENTED)
# anthropic>=0.7.0       # Claude API for LLM content generation

# Optional: PDF Generation (TO BE IMPLEMENTED)
# weasyprint>=60.0       # PDF generation from HTML

# Optional: Advanced Article Extraction
# newspaper3k>=0.2.8     # Newspaper article extraction (alternative to BS4)

# Optional: YouTube Integration
# youtube-transcript-api>=0.6.0  # Extract YouTube transcripts
# google-api-python-client>=2.100.0  # YouTube Data API

# Optional: Google Drive Integration
# google-auth>=2.23.0
# google-auth-oauthlib>=1.1.0
# google-api-python-client>=2.100.0

# Optional: Enhanced NLP
# sentence-transformers>=2.2.0  # Semantic similarity for better deduplication
# nltk>=3.8.0                   # Natural language processing

# Development Dependencies (Optional)
# pytest>=7.4.0          # Testing
# pytest-asyncio>=0.21.0 # Async testing
# mypy>=1.5.0            # Type checking
# ruff>=0.1.0            # Linting

# Installation Instructions:
#
# Minimal (Research only, no content generation):
#   pip install -r requirements-research.txt
#
# Full (With content generation and PDF):
#   pip install -r requirements-research.txt anthropic weasyprint
#
# All optional features:
#   pip install -r requirements-research.txt anthropic weasyprint youtube-transcript-api google-api-python-client
