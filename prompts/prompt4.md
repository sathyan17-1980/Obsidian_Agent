We want to ensure that the agent can only operate on folders in our vault that is specified in the env, you can see from the env.example file how we load this variable

so the agent should only be able to operate on folders in the vault
